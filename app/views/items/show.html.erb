<h1><%= @item.name %></h1>

<%= @item.description %>

<%= "$ #{@item.price}" %><br>

<%= @item.stock %><br>

<%= image_tag "#{@item.image}.jpg", size: "570x325" %>

<%= form_for @purchase do |f| %>
    <%= f.hidden_field :item_id, :value => @item.id %>  
    <%= f.submit "Add to cart" %>
<% end %>


<% @item.reviews.each do |review| %>
    <%= review.rating %>
    <%= review.body %>
<% end %>
